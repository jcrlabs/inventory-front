apiVersion: v1
kind: Service
metadata:
  name: {{ include "inventory-front.fullname" . }}
  labels:
    {{- include "inventory-front.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "inventory-front.selectorLabels" . | nindent 4 }}
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
