server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    client_max_body_size 20m;

    # Lightweight health check endpoint for Kubernetes probes
    location /health {
        access_log off;
        return 200 "ok\n";
        add_header Content-Type text/plain;
    }

    # SPA: serve index.html for all routes
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Proxy API calls to backend (for production, use ingress instead)
    location /api/ {
        proxy_pass http://inventory-back:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # Hashed assets — cache agresivo
    location ~* \.(js|css|png|jpg|jpeg|gif|woff|woff2)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # SVGs e ico sin hash en el nombre — cache corto para permitir updates
    location ~* \.(svg|ico)$ {
        expires 1d;
        add_header Cache-Control "public, max-age=86400, must-revalidate";
    }

    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml;
}
